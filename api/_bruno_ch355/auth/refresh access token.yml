info:
  name: refresh access token
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/auth/refresh"
  body:
    type: json
    data: |-
      {
        "refresh_token" : ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
