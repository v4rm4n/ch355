info:
  name: list open matches - 2
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/match/open"
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
