info:
  name: me
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/auth/me"
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
