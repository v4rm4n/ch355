info:
  name: get match state
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/match/:match_id"
  params:
    - name: match_id
      value: 4e8868be-2de9-42d1-b059-211917f26037
      type: path
  auth:
    type: bearer
    token: "{{auth_tok}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
