info:
  name: resign
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/api/match/:match_id/resign"
  params:
    - name: match_id
      value: 9f9cabfc-b662-40bd-a4d7-856660734afa
      type: path
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
