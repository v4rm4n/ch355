info:
  name: resign
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/api/match/:match_id/join"
  params:
    - name: match_id
      value: 7034220c-f520-471a-8a11-23aa530e1156
      type: path
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
