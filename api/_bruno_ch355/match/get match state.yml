info:
  name: get match state
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/match/:match_id"
  params:
    - name: match_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
