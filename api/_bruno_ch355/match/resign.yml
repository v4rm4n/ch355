info:
  name: resign
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/api/match/:match_id/resign"
  params:
    - name: match_id
      value: 15b24eac-81a6-4603-9a31-2278656e512f
      type: path
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
