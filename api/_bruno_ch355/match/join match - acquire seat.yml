info:
  name: join match - acquire seat
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/match/:match_id/join"
  params:
    - name: match_id
      value: 8242140c-33f5-466f-a18a-8c25d41dbfca
      type: path
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
