info:
  name: join match - acquire seat
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/match/:match_id/join"
  params:
    - name: match_id
      value: 9f47d70b-e468-44a7-b550-b7fac3f407e9
      type: path
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
