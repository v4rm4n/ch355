info:
  name: list open matches
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/match/open"
  auth:
    type: bearer
    token: "{{auth_tok}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
