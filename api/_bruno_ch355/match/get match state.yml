info:
  name: get match state
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/match/:match_id"
  params:
    - name: match_id
      value: 9f9cabfc-b662-40bd-a4d7-856660734afa
      type: path
  auth:
    type: bearer
    token: "{{auth_tok}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
