info:
  name: me - 2
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/auth/me"
  auth:
    type: bearer
    token: "{{auth_tok2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
